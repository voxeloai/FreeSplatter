# Cycle 2 — log

**Date:** 2026-05-08
**Actor:** pod-shell (sub-agent of architect)
**Pod:** com3tlvpfgnmmk (RTX A6000, AP-JP-1) — fresh pod after cycle-1 stop+remove; volume `ehtswxltst` reattached.

## 12:27 UTC — Connect + restore session

- SSH in at `64.247.206.204:34045` with `-o StrictHostKeyChecking=accept-new`. Host key auto-accepted.
- Volume reattached at `/workspace`; 67% used (project leaves ~293T free, but volume quota is the limit). Bash quick check: `df -h /workspace` shows the MFS endpoint healthy.
- Cycle-1 artefacts intact:
  - `/workspace/envs/freesplatter/bin/python` present.
  - `/workspace/.freesplatter-pip-installed-v1` present (zero-byte sentinel).
  - `/workspace/.local/bin/uv` present.
  - `/workspace/.ssh-state/pod_id_ed25519{,.pub}` present.
- Restored `/root/.ssh/pod_id_ed25519` from `/workspace/.ssh-state/`, chmod 600, configured `git config --global core.sshCommand` and per-repo `core.sshCommand` to use it.
- GitHub auth test: `Hi visualvlad! You've successfully authenticated` — pubkey still on Vlad's account from cycle 1, no new key action needed.
- tmux session `arch` recreated (`new-session -d -s arch -x 220 -y 50`).
- `git pull origin voxelo/main` in `/workspace/FreeSplatter`: 241a05f → 26c89b8. RECIPE.md, task.md, deploy.ps1, bootstrap.sh updated.

## Notes for handoff

- The /root/.ssh restore and git core.sshCommand setup is currently manual. install-pod-claude.sh has a baked-in `init-pod.sh` that auto-restores; the bypass-conda variant doesn't ship that. Possible candidate for a small `pod-shell-prep.sh` next to bootstrap.sh.
- The original IP was reachable on the new physical host; only the host key changed. `StrictHostKeyChecking=accept-new` handled it.

## 12:28 UTC — Bootstrap with FREESPLATTER_DOWNLOAD_WEIGHTS=1

- Apt step re-installed system libs (`libegl1`, `ninja-build`, etc.) — those don't persist across pod images, expected.
- Steps 1-6 sentinel-skipped (`Install sentinel exists; skipping pip install + kernel rebuild`).
- Step 7 downloaded TencentARC/FreeSplatter via `huggingface-cli` (the `hf` CLI isn't installed; bootstrap correctly fell back).
- Result: 3 `.safetensors` at `/workspace/weights/` (NOT `/workspace/weights/checkpoints/` as the cycle-2 task verification listing implied). Sizes ~1.23 GB each (3.6 GB total — bigger than task.md's 0.9 GB estimate).
- Sentinel `/workspace/.freesplatter-weights-complete` written. `source /workspace/activate.sh` re-emitted (no behavioural change).
- Volume usage unchanged in df — MFS shows volume-wide 67% (the per-pod quota counter is hidden). All downloads succeeded.

**Spec mismatch to flag**: cycle-2 task.md verification listing says `/workspace/weights/checkpoints/*.safetensors` — actual layout is `/workspace/weights/*.safetensors` (no `checkpoints/` subdir). Architect to correct task.md template if this matters for future repos.

## 12:30 UTC — Peer-model pre-pull attempt

Plan was: `hf download Tencent/Hunyuan3D-1 --include '*.safetensors' '*.json' '*.bin' 'config.json'` and `hf download briaai/RMBG-2.0`, both with `HF_HOME=/workspace/hf-cache`.

**Issue 1: `hf` CLI not installed.** Same as bootstrap — fell back to `huggingface-cli` with the same args (re-spelled the include flags as `--include 'X' --include 'Y'` since huggingface-cli requires per-flag).

**Issue 2: include filter only matched root-level files.** `huggingface-cli download Tencent/Hunyuan3D-1 --include '*.safetensors' --include '*.json' --include '*.bin' --include 'config.json'` pulled ONLY `config.json`. The repo layout (`mvd_lite/`, `mvd_std/`, `svrm/`) has all weight files in subdirs; `*.safetensors` doesn't cross `/` in fnmatch. Would need `**/*.safetensors` or no include filter.

**Issue 3: briaai/RMBG-2.0 is GATED.** `huggingface_hub.errors.GatedRepoError: 401 Client Error`. The model description says Access to model briaai/RMBG-2.0 is restricted. You must have access to it and be authenticated to access it. Surfaced and stopped per task.md instruction.

**Issue 4: peer-pull destination is wrong anyway.** `app.py` and `webui/runner.py` load these models with `local_dir='./ckpts/Hunyuan3D-1'` and `cache_dir='ckpts/'` (paths relative to FreeSplatter repo root) — NOT `HF_HOME`. Pre-pulling into `/workspace/hf-cache` won't help cycle 3; app.py will re-download anyway.

**Issue 5: peer model list is incomplete.** `webui/runner.py` also loads `sudo-ai/zero123plus-v1.1`, `sudo-ai/zero123plus-pipeline`, `sudo-ai/zero123plus-v1.2` — all into `./ckpts/`. Cycle-2 task.md lists only Hunyuan3D-1 + RMBG-2.0.

Decision: stopped the peer-pull. Smoke test ran without peers since FreeSplatterModel is self-contained. Surfaced full peer-model picture in handoff for architect to redesign cycle 3's peer-model strategy.

## 12:33 UTC — Smoke test all 3 configs

- First attempt: `python /tmp/smoke.py` from CWD /tmp failed with `ModuleNotFoundError: No module named 'freesplatter'` (FreeSplatter isn't pip-installed, just lives on disk). Copied script to `_smoke.py` inside the repo and ran from repo root.
- Result:
  ```
  OK: freesplatter-object instantiated, params=307.9M
  OK: freesplatter-object-2dgs instantiated, params=307.9M
  OK: freesplatter-scene instantiated, params=307.9M
  ```
- All 3 configs instantiate, .cuda(), .eval() cleanly. No checkpoint load attempted (task.md only asked for forward-pass-shape verification — model construction was the bar, not weight loading).
- `cfg.model.params` schema guess in task.md was correct.
- Cleaned up: `rm /workspace/FreeSplatter/_smoke.py` not done yet (will defer to handoff time so architect can re-run if useful).
